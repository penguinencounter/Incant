{
  "name": "lexcasting",
  "version": "1.0.0",
  "description": "DSL is in quotes because it's really just a command dispatcher.",
  "main": "index.js",
  "scripts": {
    "run": "npx tsc && node out/compiler.js"
  }
}
